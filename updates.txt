# @Author   : Tian Xiao

第七周
1、扩展了PolynomialFeatures类，使其可以应对高维度高阶数的数据（并从sklearn.datasets中加载了真实数据）
2、修改了损失函数和其梯度，使其成为真正的lasso回归（而非ridge回归）
3、实现了交叉验证和网格搜索
4、为代码增加了丰富的注释
5、探索中的功能：随机梯度下降（sgd），主成分分析（PCA）
The 7th week
1. Expanded the PolynomialFeatures class to handle high-dimensional, high-order data
(and load real data from sklearn.datasets)
2. Modify the loss function and its gradient to make it a true lasso regression (rather than ridge regression)
3. Cross-validation and grid search are implemented
4. added a rich comment for the code
5. Functions in exploration: stochastic gradient descent (sgd), principal component analysis (PCA)
